import DynamicNavBar from '@/navigator/tabs/DynamicNavBar'
import { StyleSheet, Text, View } from 'react-native'

const App = () => {
  return (
    <DynamicNavBar />
  )
}

export default App

const styles = StyleSheet.create({})